-- This is the default file for initialization
-- It must be named like the executable file, just with the extension ".lua".
-- So for a binary name "ets.exe" or "ets", this file must be called "ets.lua"

document.info("Loading ets.lua")
document.info("Loading ets.lua...done")
